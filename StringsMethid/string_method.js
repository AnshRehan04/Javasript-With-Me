let str="ansh";
let a=str.toUpperCase();
console.log(a); //ANSH
// Note:all String methods never change the original string it make copy and then apply that method to that string.
let a1=str.toLowerCase();
console.log(a1);//ansh

let str1="   rehan  ";
str1=str1.trim();
console.log(str1);  //It removes the space from start and end // rehan